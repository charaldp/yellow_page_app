version: '3'
services:
  web:
    build: .
    command: python ./django_web_app/manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    volumes:
      - .:/app/db
  frontend:
    build: ./frontend
    ports:
      - "8080:8080"
    depends_on:
      - web